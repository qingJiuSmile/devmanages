<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weds.devmanages.mapper.datasource2.DeviceBaseMange2MSMapper">

    <insert id="insertFullOperationFile2MS">
        insert into
        jreal_update_1(jdev_id,jdodata)
        values(#{devId},#{operationNumber});
    </insert>

    <insert id="insertFullOperationFileRule2MS">
        insert into
        jreal_update_2(jdev_id,jdodata)
        values(#{devId},#{operationNumber});
    </insert>

</mapper>