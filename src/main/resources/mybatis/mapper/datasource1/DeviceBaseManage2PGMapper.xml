<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weds.devmanages.mapper.datasource1.DeviceBaseManage2PGMapper">

    <insert id="insertFullOperationFile2PG">
        insert into
        jreal_update_1(jdev_id,jdodata)
        values(#{devId}::integer ,#{operationNumber});
    </insert>

    <insert id="insertFullOperationFileRule2PG">
        insert into
        jreal_update_2(jdev_id,jdodata)
        values(#{devId}::integer ,#{operationNumber});
    </insert>
</mapper>